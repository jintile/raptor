{
  "name": "RAPTOR Security Research Framework",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "runArgs": ["--privileged"],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "charliermarsh.ruff",
        "GitHub.copilot",
        "ms-vscode.cpptools",
        "GitHub.vscode-codeql"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/bin/python3",
        "python.terminal.activateEnvironment": true,
        "editor.formatOnSave": true
      }
    }
  },
  "containerEnv": {
    "GIT_TERMINAL_PROMPT": "0",
    "PYTHONUNBUFFERED": "1",
    "PYTHONDONTWRITEBYTECODE": "1"
  },
  "mounts": [
    "source=${localWorkspaceFolder}/.env,target=/workspaces/raptor/.env,type=bind,consistency=cached,readonly",
    "source=C:\\Users\\johni\\.claude,target=/root/.claude,type=bind,consistency=cached,readonly",
    "source=C:\\Users\\johni\\Fitness-App,target=/workspaces/Fitness-App,type=bind,consistency=cached,readonly"
  ],
  "postCreateCommand": "pip install -r .devcontainer/requirements-all-optional.txt && pip install -r requirements-dev.txt",
  "postStartCommand": "sudo sh -c 'echo 1 > /proc/sys/kernel/perf_event_paranoid 2>/dev/null || true'",
  "remoteUser": "vscode"
}
